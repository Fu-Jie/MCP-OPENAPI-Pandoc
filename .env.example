# Server settings
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# API Keys (comma-separated)
# Format: "sk-xxx" or "sk-xxx:2025-12-31" (with expiry date)
# Generate: python -c "import secrets; print(f'sk-{secrets.token_urlsafe(24)}')"
API_KEYS=sk-your-api-key-here:2025-12-31,sk-another-key

# Conversion settings
MAX_FILE_SIZE_MB=50
CONVERSION_TIMEOUT=60

# Service info
SERVICE_NAME=pandoc-bridge
SERVICE_VERSION=1.0.0
